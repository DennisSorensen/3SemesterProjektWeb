@model Web.Models.DepartmentListVM
@{
    ViewBag.Title = "SelectDepartment";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Vælg butik</h2>
<div>
    @foreach (var department in Model.Departments)
    {
        <p>
            @Html.ActionLink(department.Name, "SelectEmployee", "Home", new { id = department.Id }, null)
        </p>
    }
</div>

